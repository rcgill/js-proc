(function() {

o1=
  //namespace
  //
  // This is the sdoc for o1.
  ///
  // This is the single paragraph in the ldoc for o1.
  {};

o2=
  // `namespace
  //
  // This is the sdoc for o2.
  ///
  // This is the single paragraph in the ldoc for o2.
  {};

o3=
  //namespace
  // This is the sdoc for o3.
  ///
  // This is the single paragraph in the ldoc for o3.
  {};

o4=
  // `namespace
  // This is the sdoc for o4.
  ///
  // This is the single paragraph in the ldoc for o4.
  {};

o5=
  // This is the sdoc for o5.
  //
  //namespace
  //
  // This is the single paragraph in the ldoc for o5.
  {};

o6=
  // This is the sdoc for o6.
  //
  // `namespace
  //
  // This is the single paragraph in the ldoc for o6
  {};

o7=
  // This is the sdoc for o7.
  //namespace
  // This is the single paragraph in the ldoc for o7.
  {};

o8=
  // This is the sdoc for o8.
  // `namespace
  // This is the single paragraph in the ldoc for o8.
  {};

o9=
  //namespace
  //
  // This is the sdoc for o9.
  ///
  // This is the single paragraph in the ldoc for o9.
  {};

o10=
  // `namespace
  //
  // This is the sdoc for o10.
  ///
  // This is the single paragraph in the ldoc for o10.
  {};

o11=
  //namespace
  // This is the sdoc for o11.
  ///
  // This is the single paragraph in the ldoc for o11.
  {};

o12=
  // `namespace
  // This is the sdoc for o12.
  ///
  // This is the single paragraph in the ldoc for o12.
  {};

o13=
  // This is the sdoc for o13.
  //
  //namespace
  //
  // This is the single paragraph in the ldoc for o13.
  {};

o14=
  // This is the sdoc for o14.
  //
  // `namespace
  //
  // This is the single paragraph in the ldoc for o14
  {};

o15=
  // This is the sdoc for o15.
  //namespace
  // This is the single paragraph in the ldoc for o15.
  {};

o16=
  // This is the sdoc for o16.
  // `namespace
  // This is the single paragraph in the ldoc for o16.
  {};

})();
